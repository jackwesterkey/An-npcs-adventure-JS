{
  "name": "An npc's adventure",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "electron-start": "electron .",
    "esbuild-start": "esbuild src/main.js --bundle --outfile=app/main.js --watch",
    "build": "electron-packager . An npc's adventure--platform=win32 --arch=x64 --out=dist --overwrite --icon=assets/icon.ico"
  },
  "keywords": [],
  "author": "jack wester key",
  "license": "ISC",
  "devDependencies": {
    "electron": "^23.2.1",
    "electron-reloader": "^1.2.3",
    "esbuild": "^0.17.15"
  },
  "dependencies": {
    "kaboom": "^3000.1.18"
  }
}
